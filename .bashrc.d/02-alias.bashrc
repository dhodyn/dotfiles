# alias.bashrc

alias ll='ls -la'
alias l='ls'
alias h='history'
alias cp='cp -i'
alias mv='mv -i'
alias rm='rm -I'
alias mkdir='mkdir -p'
alias os='uname -r'
alias rbrc='. ~/.bashrc'
alias dotfiles='git --git-dir=${HOME}/.dotfiles_toolbox/.git --work-tree=${HOME}'

