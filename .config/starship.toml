[character]
success_symbol = "[\\$](bold green)"
error_symbol = "[\\$](bold red)"

[cmd_duration]
disabled = true

[directory]
truncation_length = 0

[hostname]
ssh_only = false
format = "[$hostname]($style):"

[username]
show_always = true
format = "[$user]($style)@"
